document:
  dsl: '1.0.3'
  namespace: fixtures
  name: emit
  version: '1.0.0'
do:
  - publish:
      emit:
        event:
          with:
            source: https://fixtures.example/workflow
            type: fixtures.demo
            data:
              value: hello
